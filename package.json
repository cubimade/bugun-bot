{
  "name": "bugun-bot",
  "version": "12.4.0",
  "description": "Instagram AI chat-bot - Claude API bilan ishlaydi",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test 'tests/**/*.test.js'",
    "minify": "npx --yes esbuild public/app.js --minify --outfile=public/app.min.js && npx --yes esbuild public/app.css --minify --outfile=public/app.min.css"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "bcryptjs": "^3.0.3",
    "compression": "^1.8.1",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
